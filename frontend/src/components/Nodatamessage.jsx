import React from 'react';

const Nodatamessage = ({ message }) => {
  return (
    <div className="text-center w-full p-4 rounded-full bg-gray-300/50 mt-4">
      {message}
    </div>
  );
};

export default Nodatamessage;
